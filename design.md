# تصميم تطبيق «حياتي»

## الغرض والنطاق

«حياتي» تطبيق خاص يعمل محليًا على الجهاز لتنظيم بيانات المستخدم وأفراد أسرته، والوثائق المرتبطة بهم، وتواريخ انتهاء الوثائق، والأحداث والمواعيد العائلية المهمة. يركز الإصدار الأول على السرعة والخصوصية؛ إذ تحفظ البيانات على الجهاز ولا يتطلب التطبيق تسجيل حساب أو اتصالًا بخدمة خارجية.

## مبادئ الواجهة

تُصمَّم التجربة للشاشة العمودية بنسبة **9:16** وللاستخدام بيد واحدة. تعتمد اللغة العربية والاتجاه من اليمين إلى اليسار، وتستخدم عناصر كبيرة قابلة للمس في أسفل الشاشة أو منتصفها. تكون الواجهة هادئة وعملية، مع بطاقات واضحة وحالات تاريخ ملوّنة تساعد المستخدم على معرفة الأولويات من النظرة الأولى، وفق نمط واجهات iOS المباشرة والمنظمة.

## الشاشات

| الشاشة | المحتوى الأساسي | الوظائف |
|---|---|---|
| الرئيسية | تحية مختصرة، ملخص عدد أفراد الأسرة والوثائق والأحداث، وقائمة بالأمور القريبة | الانتقال السريع إلى الإضافة أو إلى تفاصيل القسم |
| العائلة | قائمة أفراد الأسرة وصور رمزية وأدوارهم | إضافة فرد، تعديل بياناته، حذفه، وربط الوثائق أو الأحداث به |
| الوثائق | بطاقات للهوية والجواز والرخصة وغيرها، مع حالة التاريخ | الإضافة والتعديل والحذف وفرز الوثائق حسب قرب الانتهاء أو المالك |
| التقويم | أحداث مرتبة زمنيًا، مثل أعياد الميلاد والمواعيد المهمة | إضافة حدث أو تعديله أو حذفه والانتقال إلى الشخص المرتبط به |
| الإضافة | نموذج موحد يتيح اختيار: فرد أسرة، وثيقة، أو حدث | التحقق من الحقول وحفظ السجل محليًا ثم العودة للقسم المناسب |
| إعدادات الخصوصية | شرح أن البيانات محفوظة محليًا، مع مسح جميع البيانات بعد تأكيد صريح | عرض معلومات التخزين ومسح البيانات محليًا |

## تدفقات المستخدم الأساسية

يبدأ المستخدم من الصفحة الرئيسية، ثم يلمس زر الإضافة في أسفل الواجهة، ويختار إضافة فرد من الأسرة، ويدخل الاسم والصفة وتاريخ الميلاد اختياريًا. بعد الحفظ يظهر الفرد في تبويب العائلة ويمكن اختياره من نموذج الوثيقة أو الحدث.

عند إضافة وثيقة، يحدد المستخدم صاحبها ونوع الوثيقة ورقمها وتاريخ انتهائها. تعرض شاشة الوثائق حالة واضحة: **سارية** أو **قريبة الانتهاء** أو **منتهية**. تظهر الوثائق القريبة أو المنتهية كذلك في ملخص الصفحة الرئيسية.

وعند إضافة حدث، يكتب المستخدم العنوان وتاريخ الحدث، ويربطه بفرد من الأسرة إن لزم. يظهر الحدث ضمن قائمة التقويم، كما يظهر في الملخص إذا كان قريبًا. يمكن فتح أي بطاقة لتعديلها أو حذفها من نموذج موحد.

## الهوية البصرية

| العنصر | الاختيار | الغرض |
|---|---|---|
| اللون الأساسي | `#0E7490` أزرق بترولي | يوحي بالثقة والتنظيم ويستخدم للأزرار والعناصر النشطة |
| لون الخلفية | `#F7FAFC` أبيض مزرق | يوفر مساحة هادئة للقراءة الطويلة |
| لون البطاقات | `#FFFFFF` أبيض | فصل بسيط للمعلومات دون ازدحام |
| لون الحالة الجيدة | `#15803D` أخضر | وثائق سارية وأحداث مكتملة |
| لون التنبيه | `#D97706` كهرماني | وثائق أو أحداث خلال 30 يومًا |
| لون الحالة الحرجة | `#B91C1C` أحمر داكن | وثائق منتهية أو عناصر متأخرة |
| لون النص | `#102A43` كحلي داكن | تباين مريح وواضح للنص العربي |

## نموذج البيانات المحلي

| الكيان | الحقول الأساسية | العلاقة |
|---|---|---|
| `FamilyMember` | `id`، `name`، `relation`، `birthDate?`، `notes?`، `color` | يملك عدة وثائق وأحداث |
| `DocumentRecord` | `id`، `title`، `type`، `number?`، `ownerId?`، `issueDate?`، `expiryDate?`، `notes?` | يرتبط اختياريًا بفرد عائلة |
| `EventRecord` | `id`، `title`، `date`، `category`، `memberId?`، `notes?` | يرتبط اختياريًا بفرد عائلة |
| `AppData` | `members`، `documents`، `events` | يحفظ كوحدة واحدة داخل التخزين المحلي |

## قواعد العرض

تحسب حالة الوثيقة اعتمادًا على تاريخ الانتهاء؛ يكون السجل منتهيًا إذا مضى التاريخ، وقريبًا إذا بقي 30 يومًا أو أقل، وساريًا في غير ذلك. ترتب الأحداث القادمة أولًا، بينما ترتب الوثائق المنتهية أو القريبة أولًا لتكون المعلومات الأكثر أهمية في موضع بارز. تحفظ النماذج جميع التغييرات فورًا في التخزين المحلي، مع رسالة تأكيد قصيرة بعد كل إجراء ناجح.
